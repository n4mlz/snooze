global:
  resolve_timeout: 5m

route:
  receiver: discord
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: discord
    discord_configs:
      - webhook_url: '${ALERTMANAGER_DISCORD_WEBHOOK_URL}'
        send_resolved: true
